[build]
  command = "npx convex codegen && vite build"
  publish = "dist/client"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_CONVEX_URL,VITE_CONVEX_SITE_URL"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
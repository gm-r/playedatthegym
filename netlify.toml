[build]
  # Generate Convex client before building the app
  command = "npx convex codegen && vite build"
  # Vite outputs the production build to the dist directory by default
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888